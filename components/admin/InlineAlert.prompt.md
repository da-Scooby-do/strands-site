Save / send result line.

```jsx
<InlineAlert>Saved. The shop is showing the new details.</InlineAlert>
<InlineAlert tone="error">Nothing was saved — the price must be a number above zero.</InlineAlert>
```
