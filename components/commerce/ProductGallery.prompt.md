Product gallery for the hero's left column.

```jsx
<ProductGallery images={[{label:"The jar"},{label:"The box"},{label:"Texture"}]} />
```

Items with no `src` render a labelled placeholder ground — real photography goes in later.
