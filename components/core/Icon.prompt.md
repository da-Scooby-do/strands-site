Line-drawn icon. Lucide, 1.5px stroke, currentColor.

```jsx
<Icon name="droplet" size={20} color="var(--green)" />
```

Needs `<script src="https://unpkg.com/lucide@0.454.0/dist/umd/lucide.js">` on the page.
Never use emoji as icons.
