Comparison table — always inside a DamaskPanel.

```jsx
<ComparisonTable columns={["Strands","Salon treatment","Drugstore mask"]}
  rows={[{label:"Nine ingredients, all named", values:[true,false,false]}]} />
```

Marks only, never words in cells. Six rows maximum.
